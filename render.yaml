services:
  - type: web
    name: inventory-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
        placeholder: "Your production database connection string (e.g. postgresql://...)"
      - key: CORS_ORIGINS
        value: "*"
