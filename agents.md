# Agent Instructions

> [!IMPORTANT]
> You MUST strictly use the script `./commit-as-rrr.sh` for committing any changes to this repository. Do not use standard `git commit` directly.
> 
> Example:
> ```bash
> ./commit-as-rrr.sh "feat: add user authentication"
> ```
